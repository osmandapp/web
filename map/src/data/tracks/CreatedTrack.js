export default class CreatedTrack {

    constructor(name, points, selected, isNew) {
        this.name = name;
        this.points = points;
        this.selected = selected;
        this.isNew = isNew;
    }
}